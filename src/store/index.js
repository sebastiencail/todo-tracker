import { createStore } from 'vuex'
import tasks from './modules/tasks'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tasks
  }
})
